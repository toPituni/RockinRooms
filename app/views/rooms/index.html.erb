<h1>Rooms#index</h1>
<p>Find me HELLO HELLO in app/views/rooms/index.html.erb</p>

<ul>
  <% @rooms.each do |room| %>
  <% raise %>
    <li><%= room[:name] %> <%= room[:address] %><%= room[:price] %>
    <%= room[:description] %><%= room[:equipment] %><%= room[:district] %></li>
  <% end %>
</ul>
