<%= simple_form_for(@room) do |f| %>
  <% if @room.errors.any? %>
    <div class="errors-container">
      <ul>
        <% @room.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
<% end %>
